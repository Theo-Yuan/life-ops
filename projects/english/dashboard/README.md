# English Learning Dashboard

Static dashboard over the machine-local english learning data.

Run `python3 metrics.py --data <DATA>` to generate `metrics.json`, then `python3 -m http.server` in this directory and open `dashboard.html`.
